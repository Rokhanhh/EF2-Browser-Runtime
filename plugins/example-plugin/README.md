# Example Plugin

This disabled plugin shows the minimum local plugin structure.

To try it:

1. Set `"enabled": true` in `plugin.json`.
2. Restart the local server.
3. Open the browser console and look for `[ef-plugin:example-plugin]`.

The plugin observes `getbatch` JSON responses without modifying network traffic.
